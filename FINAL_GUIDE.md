## 🚀 دليل الاستخدام النهائي لنظام المصادقة الجديد (Google, Email, Optional Phone OTP)

**مرحباً بك في الإصدار الجديد والمحسن لنظام المصادقة!** لقد قمنا بتحويل تدفق الدخول ليكون أكثر مرونة وأماناً، مع دعم الدخول عبر جوجل والبريد الإلكتروني، وجعل التحقق من الهاتف خطوة اختيارية يمكن إجراؤها من صفحة الإعدادات.

---

### ✅ ملخص التغييرات الرئيسية:

1.  **تدفق دخول مرن:** يمكنك الآن تسجيل الدخول أو إنشاء حساب باستخدام:
    *   **البريد الإلكتروني وكلمة المرور:** مع نظام تحقق كامل.
    *   **حساب جوجل:** دخول سريع وآمن (Google OAuth).
2.  **تحقق الهاتف اختياري:** لم يعد التحقق من الهاتف إلزامياً عند التسجيل. يمكنك إجراؤه في أي وقت من صفحة الإعدادات.
3.  **صفحة إعدادات متكاملة:** صفحة `Settings` جديدة تسمح لك بإدارة معلومات حسابك، وتوثيق بريدك الإلكتروني وهاتفك.
4.  **دعم دولي للأرقام:** نظام OTP يدعم الآن أرقام الهواتف الأمريكية (+1) واليمنية (+967) تلقائياً.

---

### 🛠️ الخطوات المطلوبة منك لتفعيل النظام بالكامل:

#### 1. سحب التعديلات الأخيرة من GitHub:
افتح نافذة الأوامر (CMD) في مجلد مشروعك ونفذ الأمر التالي لسحب جميع التعديلات:
```bash
git pull origin main
```

#### 2. إعداد Supabase لمصادقة Google و Email:
   *   **تسجيل الدخول إلى Supabase:** اذهب إلى [https://app.supabase.com/](https://app.supabase.com/) وسجل الدخول إلى مشروعك.
   *   **تفعيل Email/Password:**
      1.  من القائمة الجانبية، اختر **"Authentication"**.
      2.  انتقل إلى **"Settings"**.
      3.  تأكد من تفعيل خيار **"Email Signups"**.
      4.  يمكنك أيضاً تفعيل **"Email Confirm"** إذا أردت أن يقوم المستخدمون بتأكيد بريدهم الإلكتروني بعد التسجيل (يوصى به للأمان).
   *   **تفعيل Google OAuth:**
      1.  في نفس صفحة **"Authentication"** -> **"Settings"**، انتقل إلى قسم **"Providers"**.
      2.  ابحث عن **"Google"** وقم بتفعيله.
      3.  ستحتاج إلى إدخال **`Client ID`** و **`Client Secret`** من حسابك في Google Cloud Console. (إذا لم تكن قد أعددتها، ابحث عن "How to set up Google OAuth for Supabase" للحصول على دليل مفصل).
      4.  تأكد من إضافة `http://localhost:5173/` (أو رابط موقعك المنشور) إلى **`Authorized redirect URIs`** في إعدادات Google OAuth.

#### 3. إعادة نشر دالة `send-otp` (إذا لم تكن قد فعلت ذلك بعد آخر تحديث):
```bash
npx supabase functions deploy send-otp --no-verify-jwt
```

#### 4. تشغيل الموقع محلياً:
```bash
npm run dev
```

---

### 🧪 كيفية اختبار النظام الجديد:

#### 1. اختبار الدخول عبر البريد الإلكتروني وكلمة المرور:
   *   افتح المتصفح واذهب إلى `http://localhost:5173/login`.
   *   انقر على **"إنشاء حساب جديد"**.
   *   سجل باستخدام بريد إلكتروني وكلمة مرور.
   *   بعد التسجيل، جرب الدخول بنفس البريد وكلمة المرور.

#### 2. اختبار الدخول عبر جوجل:
   *   في صفحة `http://localhost:5173/login`، انقر على زر **"الدخول عبر جوجل"**.
   *   سيتم توجيهك إلى صفحة جوجل للمصادقة.
   *   بعد المصادقة، سيتم توجيهك إلى الصفحة الرئيسية لموقعك.

#### 3. اختبار توثيق الهاتف (اختياري):
   *   بعد تسجيل الدخول (بأي طريقة)، اذهب إلى صفحة الإعدادات: `http://localhost:5173/settings`.
   *   في قسم **"رقم الهاتف"**، أدخل رقم هاتف (أمريكي `+1 774 497 1610` أو يمني `+967 733 933 331` إذا كان موثقاً في Twilio).
   *   انقر على **"إرسال رمز التحقق"**.
   *   أدخل الرمز الذي سيصلك في الحقول المخصصة وانقر على **"تأكيد الرمز"**.
   *   يجب أن يتغير حالة الرقم إلى **"مُوثَّق"**.

---

### 🎉 تهانينا!
لقد قمت الآن ببناء نظام مصادقة حديث ومتكامل ومرن لتطبيقك. هذا النظام يوفر تجربة مستخدم ممتازة مع الحفاظ على أعلى معايير الأمان.

**إذا واجهت أي مشكلة أثناء الإعداد أو الاختبار، فلا تتردد في سؤالي!**
